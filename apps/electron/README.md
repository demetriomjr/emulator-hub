# Electron

This application opens the compiled React frontend from `apps/frontend/dist/index.html`. The frontend build must exist before starting Electron. It inherits the web profile selector: a backend-owned profile is selected before a title launches, and the selected profile scopes the current EmulatorJS save namespace. The backend address and API contract are still to be specified.
